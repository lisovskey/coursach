#pragma once

#include <string>

// Генерация имени и фамилии
std::string generateName();

// Генерация группы '16 года поступления
int generateGroup();

// Генерация отметки от 4 до 10
int generateMark();

// Генерация булевого значения
bool generateBool(const int = 2);
